# =====================================================================
# Dashboard Plan Indicativo 2024-2027 — dependencias
# =====================================================================
# Pin con minimos: deja que Streamlit Cloud / pip resuelva los parches.

streamlit>=1.32
polars>=1.0
fastexcel>=0.10        # backend que polars usa para leer .xlsx
pandas>=2.1
plotly>=5.20
requests>=2.31
openpyxl>=3.1
